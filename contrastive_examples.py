
def get_label_vector_means(dataset, data):
    """Get the mean embedding for each set (pos tag, label) pair
    Args:
        dataset: TextDataSet with trained embeddings
        data: Data to include in the embedding means
    Returns:
        Dictionary (pos tag, label): embedding mean    
    """
    pass

